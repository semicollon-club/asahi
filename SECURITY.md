---
lastReviewed: 2026-07-28
---

# 보안 정책

Asahi는 소유자 개인용 비서로, 디스코드를 통해 대화한다. 실제 파일·셸 작업은 대화를 처리하는
**봇** 프로세스가 아니라, 소유자 PC(또는 소유자가 지정한 다른 PC)에서 별도로 실행되는 **워커**
프로세스가 대신 수행한다 — 봇이 워커에게 도구 호출 하나하나를 원격으로 시켜 결과만 돌려받는
얇은 위임 구조다(`docs/decisions/0006-thin-worker.md`). 이 문서는 위협 모델을 공개용으로
요약한다. 상세 능력 계층과 알려진 한계는 `docs/security/capability-model.md`,
`docs/security/risk-register.md` 를 참고한다.

## 자산

- 각 워커 고유의 토큰 — `workers` 레지스트리 테이블에 해시로만 저장되고, `register-worker`
  CLI 가 발급 시 평문을 콘솔에 한 번만 보여준다. 그 워커 하나가 "자신"임을 허브에 증명하는
  유일한 비밀이다(더 이상 봇 전체가 공유하는 고정값이 아니다). 유출되면 그 값을 아는 사람이
  **그 워커 하나**로 접속해 파일·셸 작업 결과를 조작할 수 있다.
- 워커가 노출하는 PC 폴더(`WORKER_ROOTS`)에 대한 파일 접근 권한과, 그 워커 프로세스를 돌리는
  OS 계정이 닿는 범위의 셸 실행 권한(`sh_exec`).
- 정본 상태 저장소(Postgres/Supabase) — 대화 이력, 기억, 사용자 권한, 허용 폴더 목록.
- 디스코드 봇 계정 및 소유자와의 1:1 DM 채널.
- 봇이 여는 `/worker` WebSocket 리스너 — 워커가 아웃바운드로 접속하는 통로이자, 인터넷에 노출된
  이 프로젝트의 유일한 수신 포트다.

## 주요 행위자·위협

- **손님 사용자** — 소유자가 아닌 디스코드 사용자. 등록되지 않았거나(`decideRoute` 가 애초에
  무시) `blocked` 면 아무 특권도 없다. `role='allowed'`(소유자가 `manage_access` 로 명시적으로
  등록한 동아리원)이면 대화·기억(본인 스코프)에 더해 **공유 워커가 연결돼 있으면 원격 파일/셸
  도구(`fs_*`/`sh_exec`)도 받는다** — 이건 다중 워커 1단계에서 새로 열린 표면이다
  (`docs/decisions/0007-multi-worker-routing.md`). 다만 전원 기억 조회·DB 조회·
  `manage_access`·폴더 관리(`allow_dir` 등)는 여전히 신원(`isOwner`) 전용이라 손님은 절대
  접근할 수 없다.
- **관찰 콘텐츠를 경유한 프롬프트 인젝션** — 대화 중 모델이 읽는 텍스트(첨부 이미지·파일 내용
  포함)에 숨겨진 지시가 실려 올 수 있다. 서버/스레드 채널은 참여자가 늘거나 이력을 누구나 볼 수
  있어 사설 1:1 을 보장하지 못하므로, 신뢰 경계를 별도로 둔다.
- **공개 채널 메시지 → 공유 기계 셸 실행** — 공유 워커(동아리 미니PC)가 연결된 동안,
  `role='allowed'`로 등록된 사용자라면 공개 서버 채널의 메시지 하나가 그 기계에서 셸 명령을
  실행하는 턴으로 이어질 수 있다. 사용자별 폴더 격리(`<루트>/<디스코드 userId>/`)는 실수로
  다른 사람의 폴더를 건드리는 것을 막는 안전장치이지, 마음먹고 그 밖을 보려는 사람을 막는
  경계가 아니다 — `sh_exec` 는 경로 인자 자체가 없어 이 격리를 거치지 않는다(설계상 의도,
  `docs/security/capability-model.md` "손님·공유 기계" 절). 실질적 경계는 공유 워커
  프로세스를 돌리는 미니PC 계정 하나뿐이며, 그 계정을 비관리자·최소 권한으로 두는 것이 이
  설계의 전제조건이다(`deploy/worker-셋업.md` "미니PC(윈도우) 셋업").
- **워커 토큰 유출** — 각 워커는 `workers` 레지스트리에 등록된 자기 고유의 토큰을 갖는다(더
  이상 봇 전체가 공유하는 고정값이 아니다). 토큰 하나가 새면 `/worker` 엔드포인트에 접속해
  **그 워커 하나**인 척 인증할 수 있다 — 워커별로 신원이 분리돼 있으므로 다른 워커에는 영향이
  없다. Postgres 나 소유자의 Claude 구독에는 여전히 접근하지 못한다(워커는 그 자격증명을 아예
  갖지 않는다 — 옛 버전은 DB 연결 문자열을 위임에 썼지만 얇은 워커 전환으로 없어졌다).
- **`sh_exec` 의 봉쇄되지 않는 실행 범위** — 파일 도구(`fs_read` 등)는 봇 쪽 허용 폴더 목록과
  워커 쪽 `WORKER_ROOTS` 이중 검사를 받지만, 셸 명령은 경로 인자 하나로 판정할 수 있는 대상이
  아니라서 이 검사 대상이 아니다(설계상 의도 — 파이프·서브셸·PATH 탐색 등으로 인자 검사를
  우회할 수 있기 때문이다). 신원 게이팅(`role` 이 `owner`/`allowed` 이고 그 사람이 쓸 워커가
  연결돼 있는가)으로 호출 가능한 사람만 좁힌다 — 더 이상 소유자 DM 전용이 아니다.

## 완화책 요약

| 위협 | 완화 |
| --- | --- |
| 손님이 DB 조회·접근관리·폴더관리 도구에 접근 시도 | 신원(`isOwner`) 기반 게이팅 — 역할(`role`)을 바꿔도 신원은 바뀌지 않는다. `db_query`/`db_schema`/`runtime_info`/`manage_access` 는 소유자 DM 전용(`isOwnerDm`), `allow_dir`/`revoke_dir`/`list_dirs` 는 소유자 전용(`canManagePc`)이며 각각 도구셋 계산과 핸들러 실행 두 곳에서 독립적으로 재확인한다. `docs/security/capability-model.md` §신원 vs 역할 게이팅 |
| 미등록 사용자가 원격 도구(`fs_*`/`sh_exec`)에 접근 시도 | `decideRoute` 가 `role` 이 `owner`/`allowed` 가 아닌 사용자의 메시지를 애초에 무시한다. 원격 도구 자체는 등록된 손님에게도 열리지만(1단계, `docs/decisions/0007-multi-worker-routing.md`), 도구셋 계산(`allowedToolsFor`)과 핸들러 실행(`remoteToolHandler`)이 같은 판정(`resolveTurnWorker` 하나가 채우는 `ctx.remote` 의 존재 여부)을 공유해 "도구는 보이는데 실행은 거부"가 생기지 않는다. `docs/security/capability-model.md` §신원 vs 역할 게이팅 |
| 프롬프트 인젝션으로 허용 폴더 밖 파일 접근 | 파일 도구 호출마다 봇 쪽 `allowed_dirs` 1차 필터(공유 워커의 손님은 자기 하위 폴더로 추가로 좁혀짐)와 워커 쪽 `WORKER_ROOTS`(`checkPath`, realpath 정규화) 최종 판정, 두 겹을 통과해야 한다. `docs/security/capability-model.md` §경로 게이팅 |
| 클라우드 배포에서 PC 작업이 영원히 불가능해지는 것 | PC 도구(원격 파일/셸 + 허용폴더 관리)는 배포 위치(`deployTarget`)가 아니라 "이 턴이 쓸 워커가 지금 연결돼 있는가"(`workerConnected`) 하나로 결정한다 — 클라우드에 떠 있어도 워커만 연결되면 PC 작업이 가능하다. `docs/security/capability-model.md` §능력 계층표 |
| 워커 토큰 유출 | 사람이 고르지 않고 `register-worker` 가 32바이트 랜덤으로 발급하며, 평문은 저장하지 않는다(`sha256` 해시만). 허브는 빈 토큰을 해시 비교 전에 거부하고, 상수 시간 비교로 타이밍 공격을 방지하며, 미등록 워커 ID·토큰 불일치를 같은 거부 사유로 응답해 인증 오라클을 방지한다. 각 워커가 자기 고유 토큰을 가지므로 유출 범위는 그 워커 하나로 제한된다. `docs/security/risk-register.md` §1 |
| `sh_exec` 의 봉쇄되지 않는 실행 범위 | 경로로 막지 않는다(설계상 불가능) — 신원 게이팅으로 호출 가능한 사람을 좁히고, 워커를 최소 권한 OS 계정으로 운영하며 `WORKER_ROOTS` 를 실제로 노출해도 되는 폴더로 좁히는 운영상 완화에 의존한다. `docs/security/risk-register.md` §5 |
| 임의 SQL 실행 시도 | 애플리케이션 사전검사 + Postgres READ ONLY 트랜잭션의 이중 방어. `docs/security/capability-model.md` §READ ONLY SQL 가드 |

## 취약점 보고

보안 취약점을 발견했다면 이 저장소의 GitHub Security Advisories(비공개 신고)로 알려 주세요.
