---
lastReviewed: 2026-07-26
---

# 보안 정책

Asahi는 소유자 개인용 비서로, 디스코드를 통해 대화한다. 실제 파일·셸 작업은 대화를 처리하는
**봇** 프로세스가 아니라, 소유자 PC(또는 소유자가 지정한 다른 PC)에서 별도로 실행되는 **워커**
프로세스가 대신 수행한다 — 봇이 워커에게 도구 호출 하나하나를 원격으로 시켜 결과만 돌려받는
얇은 위임 구조다(`docs/decisions/0006-thin-worker.md`). 이 문서는 위협 모델을 공개용으로
요약한다. 상세 능력 계층과 알려진 한계는 `docs/security/capability-model.md`,
`docs/security/risk-register.md` 를 참고한다.

## 자산

- `WORKER_TOKEN` — 워커가 "소유자의 워커"임을 봇에게 증명하는 유일한 비밀. 유출되면 그 값을
  아는 사람이 워커인 척 접속해 파일·셸 작업 결과를 조작할 수 있다.
- 워커가 노출하는 PC 폴더(`WORKER_ROOTS`)에 대한 파일 접근 권한과, 그 워커 프로세스를 돌리는
  OS 계정이 닿는 범위의 셸 실행 권한(`sh_exec`).
- 정본 상태 저장소(Postgres/Supabase) — 대화 이력, 기억, 사용자 권한, 허용 폴더 목록.
- 디스코드 봇 계정 및 소유자와의 1:1 DM 채널.
- 봇이 여는 `/worker` WebSocket 리스너 — 워커가 아웃바운드로 접속하는 통로이자, 인터넷에 노출된
  이 프로젝트의 유일한 수신 포트다.

## 주요 행위자·위협

- **손님 사용자** — 소유자가 아닌 디스코드 사용자. 대화·기억(본인 스코프)만 가능해야 하며,
  PC 도구·전원 기억 조회·DB 조회 같은 특권에 접근할 수 없어야 한다. 워커가 연결돼 있어도
  손님에게는 원격 도구 자체가 노출되지 않는다.
- **관찰 콘텐츠를 경유한 프롬프트 인젝션** — 대화 중 모델이 읽는 텍스트(첨부 이미지·파일 내용
  포함)에 숨겨진 지시가 실려 올 수 있다. 서버/스레드 채널은 참여자가 늘거나 이력을 누구나 볼 수
  있어 사설 1:1 을 보장하지 못하므로, 신뢰 경계를 별도로 둔다.
- **`WORKER_TOKEN` 유출** — 이 값 하나로 `/worker` 엔드포인트에 접속해 소유자의 워커인 척
  등록할 수 있다. Postgres 나 소유자의 Claude 구독에는 접근하지 못하지만(워커는 그 자격증명을
  아예 갖지 않는다 — 옛 버전은 DB 연결 문자열을 위임에 썼지만 얇은 워커 전환으로 없어졌다), 그
  워커가 노출한 폴더의 파일 작업과 그 프로세스 권한 범위의 셸 실행은 가로챌 수 있다.
- **`sh_exec` 의 봉쇄되지 않는 실행 범위** — 파일 도구(`fs_read` 등)는 봇 쪽 허용 폴더 목록과
  워커 쪽 `WORKER_ROOTS` 이중 검사를 받지만, 셸 명령은 경로 인자 하나로 판정할 수 있는 대상이
  아니라서 이 검사 대상이 아니다(설계상 의도 — 파이프·서브셸·PATH 탐색 등으로 인자 검사를
  우회할 수 있기 때문이다). 신원 게이팅(소유자 DM + 워커 연결)으로 호출 가능한 사람만 좁힌다.

## 완화책 요약

| 위협 | 완화 |
| --- | --- |
| 손님이 PC/특권 도구에 접근 시도 | 신원(`isOwner`) 기반 게이팅 — 역할(`role`)을 바꿔도 신원은 바뀌지 않는다. 원격 도구는 소유자 DM 에서만, 도구셋 계산과 핸들러 실행 두 곳에서 각각 재확인한다. `docs/security/capability-model.md` §신원 vs 역할 게이팅 |
| 프롬프트 인젝션으로 허용 폴더 밖 파일 접근 | 파일 도구 호출마다 봇 쪽 `allowed_dirs` 1차 필터와 워커 쪽 `WORKER_ROOTS`(`checkPath`, realpath 정규화) 최종 판정, 두 겹을 통과해야 한다. `docs/security/capability-model.md` §경로 게이팅 |
| 클라우드 배포에서 PC 작업이 영원히 불가능해지는 것 | PC 도구(원격 파일/셸 + 허용폴더 관리)는 배포 위치(`deployTarget`)가 아니라 "그 소유자의 워커가 지금 연결돼 있는가"(`workerConnected`) 하나로 결정한다 — 클라우드에 떠 있어도 워커만 연결되면 PC 작업이 가능하다. `docs/security/capability-model.md` §능력 계층표 |
| `WORKER_TOKEN` 유출 | 최소 20자 무작위 값을 강제(짧거나 빈 값이면 시작 자체를 거부), 상수 시간 비교로 타이밍 공격을 방지, 토큰 오류·신원 불일치를 같은 거부 사유로 응답해 인증 오라클을 방지한다. `docs/security/risk-register.md` §1 |
| `sh_exec` 의 봉쇄되지 않는 실행 범위 | 경로로 막지 않는다(설계상 불가능) — 신원 게이팅으로 호출 가능한 사람을 좁히고, 워커를 최소 권한 OS 계정으로 운영하며 `WORKER_ROOTS` 를 실제로 노출해도 되는 폴더로 좁히는 운영상 완화에 의존한다. `docs/security/risk-register.md` §5 |
| 임의 SQL 실행 시도 | 애플리케이션 사전검사 + Postgres READ ONLY 트랜잭션의 이중 방어. `docs/security/capability-model.md` §READ ONLY SQL 가드 |

## 취약점 보고

보안 취약점을 발견했다면 이 저장소의 GitHub Security Advisories(비공개 신고)로 알려 주세요.
