---
title: 로드맵
lastReviewed: 2026-08-07
---

# 로드맵

현재 상태(라이브·병합 기능·미완 항목)는 [STATUS.md](STATUS.md) 참고. 이 문서는 **다음에 무엇을
할 것인가**만 다룬다.

## 상위 테마 — 무엇을 만들고 있는가

2026-08-05 캐릭터 페르소나 제거([설계](../superpowers/specs/2026-08-05-remove-character-persona-design.md))로
**상위 테마가 바뀌었다.** 그전까지 이 문서는 "AI를 하나의 인격체로 느끼게" 만드는 인격체화 3축
(캐릭터·자기인지·능동성)으로 전체를 조직했는데, 그중 1축(캐릭터)을 통째로 걷어냈다. 남은 두 축을
"캐릭터가 발현되는 구체적 행동"으로 설명하던 근거도 함께 사라졌다 — **발현될 캐릭터가 없다.**

지금의 아사히는 기둥 둘이다.

1. **원격 개발 비서** — 디스코드에서 워커의 파일·셸·장기 실행 프로세스를 조종한다. 실제로 만들어진
   표면 중 가장 크다(원격 도구 11종, [아키텍처 개요](../architecture/overview.md)).
2. **동아리 지식 저장소** — 공용 기억(`scope='shared'`)과 정기 게시. 캐릭터 제거 스펙이 "남는 핵심
   기능"으로 명시한 것이다.

**자기 관측**(자기인지·`actions` 기록)은 세 번째 기둥이 아니라 위 둘을 진단하는 가로축으로 취급한다.

## 지금의 사용 국면 — 소유자 단독

동아리 미니PC(공유 워커)는 2026-08-01부터 가동 중이지만 **부원에게는 아직 열지 않았다**(2026-08-07
확인). 이 사실이 아래 우선순위를 지배한다.

- 손님 관련 위험(공용 기억 유실, `sh_exec` 폴더 격리 우회)은 **아직 실현되지 않았다** — 급하지 않다.
- 대신 "혼자 빠르게 고치는" 활동을 받치는 것(CI·개발 도구로서의 실효)이 가장 값이 크다.
- 부원 오픈은 날짜가 아니라 **게이트**로 관리한다(아래 별도 절).

## Now — 2026-09-05 갱신

2026-08-07 의 Now 셋은 끝났다. 1번은 [`.github/workflows/agent.yml`](../../.github/workflows/agent.yml)(리눅스·
윈도우 매트릭스)로, 2번은 발행·되받기(2026-08-07)에 이어 2026-09-05 의 셸 git 자격증명·PR 생성·표준 작업
절차·PR 추적까지로 "디스코드만으로 clone → 브랜치 → 커밋 → push → PR" 이 실사용 확인됐다. 그 확인이 다음
Now 를 정했다.

| # | 항목 | 왜 지금인가 |
|---|---|---|
| N1 | **풀 하네스 · 미니PC 단일 호스트** ([설계](../superpowers/specs/2026-09-05-full-harness-worker-design.md) · [계획](../superpowers/plans/2026-09-05-full-harness-worker.md)) | 운영자의 목표는 "디스코드에서 Claude Code 처럼 막힘 없이" 인데, 얇은 워커는 능력 하나마다 원격 도구 한 벌을 요구한다 — 이미지 생성·파일 반환·DB·브라우저·캡처·개발 밖 스킬이 전부 막혀 있다. Claude Code 를 미니PC 에서 통째로 돌리고, 봇도 같은 기계의 다른 계정으로 옮겨 Railway 를 내린다. 자격증명은 봇 계정의 루프백 프록시가 끼운다 — 세션 계정에는 없다. 새 능력이 코드가 아니라 구성이 된다. 1단계는 봇 이사(기계 두 대의 함정 제거), 2단계부터 하네스. Next 7(파일 내려받기)·8(스크린샷)과 Later 의 스킬 하네스 2단계는 이 스펙의 0·4단계에 흡수됐다 |
| N2 | **파일 반환**(위 스펙 0단계) — **구현 완료(2026-09-05 저녁), 스모크 미검증** | 풀 하네스와 무관하게 먼저 값이 있다 — 만든 이미지·PDF 가 디스코드로 돌아온다. `send_file` → 봇 `POST /files`(작업 토큰) → 첨부. 1MB 프레임 한계는 HTTP 엔드포인트로 우회했다 |

| 지난 Now | 처리 |
|---|---|
| CI 에 테스트·타입체크 | 완료(2026-08-07) |
| 깃허브 발행 | 완료(2026-08-07 발행·되받기 → 2026-09-05 git 워크플로우 1·2단계) |
| 이 문서 재작성 | 완료(2026-08-07), 2026-09-05 Now 갱신 |

### Now 2번이 스킬 하네스 2단계를 밀어낸 경위

이 문서의 첫 판(2026-08-07 오전)은 **스킬 하네스 2단계**를 Now 2번에 뒀다. 조사해 보니
[스킬 하네스 설계](../superpowers/specs/2026-08-01-skills-harness-design.md) §7이 그 단계에
전제조건을 걸어 뒀다 — *"1이 끝나야 어떤 스킬이 스크립트를 필요로 하는지를 실물로 안다.
추측으로 설계하면 안 쓰는 것을 만든다."*

**그 전제가 안 채워져 있었다.** 설치된 스킬은 `frontend-design` 하나뿐이고 스크립트 파일은
0개다. 그 상태로 설계하면 존재하지 않는 스킬을 위한 규약을 만드는 것이라, §7이 경고한 바로
그 실패가 된다. 항목을 내리고 게이트가 없는 일로 바꿨다.

문서 스킬(pdf/docx/xlsx)을 들여 전제를 채우는 길도 막혔다 — 그 넷은 같은 리포의 다른 스킬과
달리 오픈소스가 아니고, 라이선스가 *"retain copies of these materials outside the Services"*
를 금지한다. 이 리포의 스킬 설치 절차는 폴더를 복사해 커밋하는 것이라 그 금지에 정면으로
해당하고, 리포가 공개라 재배포까지 된다.

## Next — 1~2개월

| # | 항목 | 메모 |
|---|---|---|
| 4 | **2D 백업 — 기억·대화 정기 내보내기** | `backups` 테이블은 DDL만 있고 배선이 없다. 부원 오픈의 전제조건이지만 그 전에도 필요하다 — 소유자 개인 기억도 지금은 복구 경로가 0이다([smoke-test.md](../../deploy/smoke-test.md)의 "공용 기억은 백업이 없어 복구할 방법이 없다") |
| 5 | **자기인지 조각B 완결 — `actions` 자기조회 도구** | 기록은 이미 흐른다(관측 M1). 없는 것은 읽는 경로뿐이라 지금은 소유자 DM의 `db_query`로 우회한다. **원래 M2 계획은 축소해서 받는다** — 아래 참고 |
| 6 | **스모크 미검증 항목 소진** | [smoke-test.md](../../deploy/smoke-test.md)의 체크박스가 전부 비어 있다. 우선순위 셋: 워커 최종 게이트(`checkPath` — 봇 쪽 1차 필터에 먼저 걸려 **실환경에서 한 번도 도달한 적이 없다**), 재부팅 후 PM2 데몬 독립 기동, 디스코드 파일 업로드 e2e |
| 7 | **범용 파일 내려받기 (워커 → 디스코드)** — **완료(2026-09-05, Now N2 파일 반환)** | 봇의 `POST /files` 로 1MB 프레임을 우회했다(청크 분할도 외부 스토리지도 아니다). 원래 메모: 올리기만 있고 그 반대가 없다. `MAX_FRAME_CHARS`(1MB)가 허브와 전송 계층 양쪽에서 강제돼, 기존 `result` 프레임에 base64로 실으면 실효 상한이 약 750KB다 — `attachments.ts`가 8MB를 고른 이유("올라갔는데 못 돌려받는 파일이 생긴다")와 충돌한다. 청크 분할이나 외부 스토리지 경유가 필요하다. 깃허브 발행이 프로젝트는 덮지만, 생성한 PDF 한 장 같은 산출물은 여전히 못 돌려준다 |
| 8 | **스크린샷 — 개발서버 화면 확인** | 발행 → 배포는 피드백 루프가 분 단위라, UI를 몇 번씩 고쳐가며 다듬을 땐 여전히 느리다. 워커에 헤드리스 브라우저를 두고 `proc_start`로 띄운 개발서버를 찍는다. 7번(전송 경로)에 의존한다. Apache-2.0인 `webapp-testing` 스킬이 정확히 이 일을 하므로, 스킬 하네스 2단계의 전제를 채우는 첫 실물 후보이기도 하다 |

### 관측 M2를 축소해서 받는 이유

[관측 설계](../superpowers/specs/2026-07-28-observability-for-club-open-design.md) §9는 M2를 "`actions`를
열어 어떤 도구가 자주 실패하는가·**부원이 어디서 멈추는가**를 보고 우선순위를 정한다"로 잡았다.
**부원이 없으면 그 질문의 절반은 물을 수 없다** — 쌓인 데이터는 소유자 자신의 사용뿐이다.

따라서 5번의 목적을 "부원 사용 분석"이 아니라 **소유자의 자기 진단**(내 요청이 어디서 실패했나)으로
좁혀 진행하고, 원안의 M2 결정 지점은 부원 오픈 뒤로 미룬다. 추측으로 우선순위를 정하지 않겠다는
그 문서의 원칙은 그대로 지킨다.

## Later

- **CI/CD 연결** — Now 2번(깃허브 발행)의 다음 단계. 발행된 프로젝트가 자동으로 배포돼 실제로 도는
  주소가 된다. 발행이 먼저 돌아야 무엇을 배포할지가 실물로 정해지므로 순서를 뒤집지 않는다.
  비공개 리포를 골랐으므로(설계 §4) 무료 조직에서는 GitHub Pages를 못 쓴다 — Vercel·Netlify 등
  비공개 리포를 무료로 받는 곳을 쓴다
- **스킬 하네스 2단계 — 워커 측 스크립트 실행** — 전제조건(어떤 스킬이 스크립트를 필요로 하는지)이
  아직 안 채워졌다(위 "Now 2번이 …" 참고). Next 8번(스크린샷)이 `webapp-testing`을 실물로 들이면
  그때 이 항목의 근거가 생긴다
- **2E 능동성** — 스스로 판단해 먼저 행동한다(능동적 알림·제안). `triggers` 테이블이 DDL만 있는 채로
  기다리고 있고, 정기 뉴스 게시가 사실상 첫 프로토타입이다(시각 판정 + 하루 3회 상한 + 실패 안내
  억제까지 이미 겪었다). 원래의 5단계 로드맵에서 "3단계 스케줄러/감지"에 해당한다
- **공유 워커 다중화 · 스레드↔프로젝트 폴더 바인딩** — 지금 라우팅은 가장 먼저 등록된 `shared` 워커
  하나만 고른다. 다중 워커 2단계의 몫이다
- **웹 UI** — 디스코드 외 채널. 이벤트버스가 이미 어댑터를 분리해 둬서 코어를 건드리지 않고 붙는다.
  원래 5단계 로드맵의 4단계

## 부원 오픈 게이트

날짜로 잡지 않는다. 아래가 전부 닫히면 연다.

- [ ] **2D 백업**(Next 4) — 부원이 쓴 공용 기억은 복구 불가능한 유일한 데이터다
- [ ] **`sh_exec` 격리를 사람 눈으로 실검증** — 폴더 격리는 실수 방지선일 뿐이고 실질 경계는 미니PC의
      비관리자 OS 계정 하나다([risk-register](../security/risk-register.md) §2·§5). 이건 코드로 고칠
      수 있는 항목이 아니라 **운영으로만 줄어드는** 잔여 위험이라, 열기 전에 그 계정 상태를 직접
      확인해야 한다
- [ ] **워커 최종 게이트 실도달 확인**(Next 6) — 봇 쪽 1차 필터가 먼저 걸려 워커의 `checkPath`가 실제로
      집행되는 것을 아직 못 봤다
- [ ] `/help` 손님 항목과 진행 표시가 실사용에서 읽히는지 — 관측 M1이 만든 것을 손님 눈으로 한 번

열고 나면 원안의 **M2 결정 지점**이 비로소 의미를 갖는다 — `actions`에 쌓인 실제 질의로 그다음
우선순위를 정한다.

## 내린 것 (2026-08-07)

| 항목 | 처리 | 이유 |
|---|---|---|
| **인격체화 3축 프레이밍** | 폐기 | 1축(캐릭터)이 삭제돼 프레이밍이 성립하지 않는다. 위 "상위 테마"로 교체 |
| **자기인지 조각A(사용량 자각)** | 조각B에 흡수 | `turns`·`actions` 두 테이블이 이미 그 데이터를 갖고 있어 별도 항목일 이유가 없다 |
| **손님 PC 접근(Phase B)** | 오픈 게이트 뒤로 | OS 격리 선행 과제가 그대로다. 부원 오픈조차 아직인 지금은 두 단계 앞이다 |
| **2C 관측** | 완료 처리 | `actions` 로깅·진행 표시·`fs_tree`·`/help`까지 M1 범위가 끝났다. 남은 조각(자기조회)은 Next 5로 옮겼다. `logs` 테이블 기반 SDK 훅 에러 수집은 배선된 적이 없고, 지금은 `actions`가 그 자리를 대신하므로 되살리지 않는다 |

## 5단계 로드맵 (원안, 기록용)

시작 시점의 큰 그림이다. 진행하며 순서를 재정의했다(자기인지·멀티유저를 파일셸/브라우저보다 앞당겼다).

1. **코어/디스코드** — 완료
2. **파일셸/브라우저** — 멀티유저 런타임·자기인지·원격 개발 워크플로우로 재정의되어 대부분 완료.
   손님용 PC 접근(Phase B)만 남았고 오픈 게이트 뒤다
3. **스케줄러/감지** — 위 2E 능동성
4. **웹 UI** — Later
5. **장기 목표/GUI** — 더 먼 확장 방향
